
@{
    ViewData["Title"] = "Form";
}

@model LaytonTemple.Models.ViewModels.GroupView


<p>Thank you for choosing a time!</p>
<p>Your scheduled time is @Model.Timeslot.TimeSlot</p>

<form method="post" asp-controller="Home" asp-action="Form">
    <div asp-validation-summary="All" class="text-danger"></div>

    @* Form for gathering Group Information *@
    @*<input class="form-control" type="hidden" value="Model.Timeslot.TimeSlot" asp-for="groupinfo.timeslot" />*@

    <h4>Group Information</h4>
    <div class="form-group">
        <label asp-for="@Model.groupinfo.groupName">Name: </label>
        <input asp-for="@Model.groupinfo.groupName" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="@Model.groupinfo.groupSize">Group Size: </label>
        <input asp-for="@Model.groupinfo.groupSize" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="@Model.groupinfo.email">Email Address: </label>
        <input asp-for="@Model.groupinfo.email" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="@Model.groupinfo.phone">Phone Number: </label>
        <input asp-for="@Model.groupinfo.phone" class="form-control" />
    </div>
            
    <button type="submit" class="btn btn-success btn-sm-float-end">Confirm Appointment</button>
     
</form>
